---
"@aligent/cdk-static-hosting": minor
---

Adds cloudFrontLogRetentionDays prop to enable automatic deletion of CloudFront access logs after a specified number of days using S3 lifecycle rules. Logs are retained indefinitely by default for backward compatibility.
